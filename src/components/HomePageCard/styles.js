import styled from 'styled-components';

export const CardWrapper = styled.div`
  h2 {
    font-size: 2.4rem;
    text-decoration: underline;
    cursor: pointer;
    color: black;
  }

  p {
    font-size: 1.8rem;
    line-height: 160%;
  }
`;
